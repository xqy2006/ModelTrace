__pycache__/
.pytest_cache/
*.pyc
*.log
.venv/
node_modules/

# GUI evidence is produced by `python tests/capture_gui_evidence.py` against a
# running instance; the screenshots and manifest are verification output, not
# sources, so they are never committed.
orca-evidence/
